name: Get latest release version
on:
  schedule:
    - cron:  "*/5 * * * *"
jobs:
  get-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v1
      - run: pip index versions transparentpath
